import { types } from "..";
/**
 * note: if you get this data from the api,
 * put the Change in water balance map after the Change in wildfire danger days map
 * until it is reordered from the api side.
 * */
export declare const datasets: types.Map[];
