/**
 * Receives an array and a field
 * Returns an object with all elements of the array "data" being grouped by "field"
 */
export declare const groupByfield: <T>(data: T[], field: string) => {
    [key: string]: {
        label: string;
        options: T[];
    };
};
export declare const degreeToString: (value: number) => string;
